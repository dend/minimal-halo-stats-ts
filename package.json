{
  "name": "minimal-halo-stats",
  "version": "1.0.0",
  "description": "A minimal CLI to display your last 10 Halo Infinite matches",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "parse-film": "tsc && node dist/parse-film.js"
  },
  "dependencies": {
    "@dendotdev/conch": "1.0.1",
    "@dendotdev/grunt": "1.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
